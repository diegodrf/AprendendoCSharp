using System;

namespace P10_LacoDeRepeticaoFor
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Quando se tem apenas uma linha a ser executada, não é necessário
               Utilizar {}.
               if, else, for, while */
            for (int contador = 0; contador <= 10; contador++)
                Console.WriteLine(contador);
        }
    }
}
